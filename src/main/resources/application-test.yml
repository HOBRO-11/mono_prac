spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: password

  jpa:
    # hibernate:
      # ddl-auto: create
    properties:
      hibernate:
        '[format_sql]': true
        '[show_sq]': true
  
  data:
    redis:
      host: 127.0.0.1
      port: 6379